version: "3.4"

services:
  grannex:
    image: grannex
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        SECRET_KEY: ${SECRET_KEY}
    environment:
      - SECRET_KEY=${SECRET_KEY}
    ports:
      - 8000:8000
